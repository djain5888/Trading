# Development dependencies. Mirrors [project.optional-dependencies.dev].
# Includes runtime requirements via the -r directive below.

-r requirements.txt

ruff>=0.7
black>=24.10
mypy>=1.13
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
